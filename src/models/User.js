const User = {
  // name: DS.attr("string"),
  // age: DS.attr("number"),
  // isPublic: DS.attr("boolean"),
  // birthDate: DS.attr("date"),
  // skills: DS.hasMany("skill")
  firstName: 'string',
  lastName: 'string',
  age: 'number'
}

export default User
